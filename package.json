{
  "name": "lafoliedouce-monorepo",
  "version": "1.0.0",
  "author": "Hugo Heynard",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "turbo run build",
    "dev:infra": "docker compose -f docker-compose.dev.yml up -d",
    "dev:infra:down": "docker compose -f docker-compose.dev.yml down",
    "dev:infra:nuke": "docker compose -f docker-compose.dev.yml down -v",
    "dev": "turbo run dev",
    "chevallot-suite:dev:watch": "turbo run dev --filter=chevallot-pim-frontend --filter=chevallot-pim-backend",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.0",
    "typescript": "~5.9.3"
  }
}
