/**
 * Query **admin** : lister toutes les sociétés (cross-tenant), pour l'onglet
 * « Comptes clients » du staff commercial. Aucun paramètre — l'autorisation est
 * portée par le guard staff, pas par la query.
 */
export class ListAllCompaniesQuery {}
