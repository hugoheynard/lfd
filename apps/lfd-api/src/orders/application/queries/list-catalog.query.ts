/**
 * Le catalogue entier, tel qu'un écran le parcourt.
 *
 * Sans paramètre, et c'est le point : c'est **le** catalogue, celui-là même dont
 * le checkout ré-résout les prix. Le filtrer côté serveur donnerait deux vérités
 * selon la question posée.
 */
export class ListCatalogQuery {}
