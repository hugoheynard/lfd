/**
 * Query : lire la queue **« 5 meilleurs coups du jour »** (read-model `lead_score`)
 * pour l'onglet Tableau de bord. Effet de bord assumé côté handler : journaliser
 * `reco.shown` (capture de la boucle fermée, Phase 2).
 */
export class GetCockpitQuery {}
