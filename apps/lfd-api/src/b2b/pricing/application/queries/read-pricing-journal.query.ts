/**
 * « Qui a touché aux prix, dernièrement ? » — tous sujets confondus.
 *
 * Sans paramètre : la profondeur du fil est une décision de lecture, pas une
 * option de l'appelant. Au-delà, ce n'est plus une histoire, c'est un fichier de
 * logs.
 */
export class ReadPricingJournalQuery {}
