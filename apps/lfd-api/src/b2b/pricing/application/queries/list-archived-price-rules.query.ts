/**
 * « Qu'a-t-on retiré ? » — l'autre question du tableau, celle que la lecture
 * courante ne pose pas.
 *
 * Séparée de {@link ReadPricingBoardQuery} plutôt que rendue par un drapeau :
 * mêler les règles rangées à celles qui s'appliquent alourdirait chaque nœud de
 * l'écran pour un besoin qu'on a trois fois par an.
 */
export class ListArchivedPriceRulesQuery {}
