/**
 * Query : **la prochaine journée demandable**, pour chaque destination de
 * service. Aucun paramètre — la réponse couvre tous les points de retrait plus
 * le défaut de la plateforme, et elle est la même pour tout le monde.
 */
export class ListFulfillmentDaysQuery {}
