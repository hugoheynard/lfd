/**
 * URLs des apps hostées — version **DEV** (localhost). Substituée à
 * `suite-config.ts` par la configuration `development` d'angular.json.
 */
export const SUITE_APP_URLS: Readonly<Record<string, string>> = {
  pim: 'http://localhost:7315',
};
